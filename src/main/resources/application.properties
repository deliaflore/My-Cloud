spring.application.name=stumatch-distributed
server.port=8081

# Storage settings
storage.base-dir=${STORAGE_BASE_DIR:${user.home}/distributed-storage}
storage.user-dir=${STORAGE_USER_DIR:users}

# PostgreSQL datasource configuration (override via environment variables as needed)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/stumatch_cloud}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT settings
jwt.secret=${JWT_SECRET:your-256-bit-secret-key-change-this-in-production-minimum-32-characters-long}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email settings (optional - if not configured, OTP will be logged to console)
# For Gmail: Use App Password (not regular password) - see EMAIL_SETUP.md
# Quick setup: Set MAIL_USERNAME and MAIL_PASSWORD environment variables
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:noblesseb7@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ajqn uluu jodd djwi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Optional: Set from address (defaults to username)
spring.mail.properties.mail.smtp.from=${MAIL_FROM:${spring.mail.username}}
